/**
 * Seed catalog for Algorithm Memorizer — classic CS algorithms (not LeetCode problems).
 * Tests use `expression` (Python call) and `expected` (Python literal / repr match).
 */

export const ALGORITHM_SEED_VERSION = 2

export const ALGORITHM_SEED = [
  {
    slug: 'binary-search',
    title: 'Binary Search',
    category: 'searching',
    difficulty: 'easy',
    description: 'Search a sorted ascending list for target and return its index, or -1 if absent.',
    skeleton: `def binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1\n`,
    reference: `def binary_search(nums, target):\n    lo, hi = 0, len(nums) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1\n`,
    testCases: [
      { id: 'v1', name: 'found', visible: true, expression: 'binary_search([1, 3, 5, 7, 9], 7)', expected: '3' },
      { id: 'v2', name: 'missing', visible: true, expression: 'binary_search([1, 3, 5, 7, 9], 2)', expected: '-1' },
      { id: 'h1', name: 'edges', visible: false, expression: 'binary_search([2, 4, 6], 2)', expected: '0' },
    ],
    timeComplexity: 'O(log n)',
    spaceComplexity: 'O(1)',
    order: 10,
  },
  {
    slug: 'breadth-first-search',
    title: 'Breadth-First Search',
    category: 'graphs',
    difficulty: 'medium',
    description: 'Traverse an undirected graph (adjacency dict of lists) from start in BFS order; return the visit list.',
    skeleton: `from collections import deque\n\ndef bfs(graph, start):\n    visited = []\n    q = deque([start])\n    seen = {start}\n    while q:\n        node = q.popleft()\n        visited.append(node)\n        for nei in graph.get(node, []):\n            if nei not in seen:\n                seen.add(nei)\n                q.append(nei)\n    return visited\n`,
    reference: `from collections import deque\n\ndef bfs(graph, start):\n    visited = []\n    q = deque([start])\n    seen = {start}\n    while q:\n        node = q.popleft()\n        visited.append(node)\n        for nei in graph.get(node, []):\n            if nei not in seen:\n                seen.add(nei)\n                q.append(nei)\n    return visited\n`,
    testCases: [
      { id: 'v1', name: 'line', visible: true, expression: 'bfs({"A": ["B"], "B": ["A", "C"], "C": ["B"]}, "A")', expected: "['A', 'B', 'C']" },
      { id: 'v2', name: 'star', visible: true, expression: 'bfs({"A": ["B", "C"], "B": ["A"], "C": ["A"]}, "A")', expected: "['A', 'B', 'C']" },
      { id: 'h1', name: 'single', visible: false, expression: 'bfs({"X": []}, "X")', expected: "['X']" },
    ],
    timeComplexity: 'O(V + E)',
    spaceComplexity: 'O(V)',
    order: 20,
  },
  {
    slug: 'depth-first-search',
    title: 'Depth-First Search',
    category: 'graphs',
    difficulty: 'medium',
    description: 'Traverse an undirected graph from start in recursive DFS order; return the visit list.',
    skeleton: `def dfs(graph, start):\n    visited = []\n    seen = set()\n\n    def go(node):\n        seen.add(node)\n        visited.append(node)\n        for nei in graph.get(node, []):\n            if nei not in seen:\n                go(nei)\n\n    go(start)\n    return visited\n`,
    reference: `def dfs(graph, start):\n    visited = []\n    seen = set()\n\n    def go(node):\n        seen.add(node)\n        visited.append(node)\n        for nei in graph.get(node, []):\n            if nei not in seen:\n                go(nei)\n\n    go(start)\n    return visited\n`,
    testCases: [
      { id: 'v1', name: 'line', visible: true, expression: 'dfs({"A": ["B"], "B": ["A", "C"], "C": ["B"]}, "A")', expected: "['A', 'B', 'C']" },
      { id: 'v2', name: 'branch', visible: true, expression: 'dfs({"A": ["B", "C"], "B": ["A"], "C": ["A"]}, "A")', expected: "['A', 'B', 'C']" },
      { id: 'h1', name: 'single', visible: false, expression: 'dfs({"Z": []}, "Z")', expected: "['Z']" },
    ],
    timeComplexity: 'O(V + E)',
    spaceComplexity: 'O(V)',
    order: 30,
  },
  {
    slug: 'bubble-sort',
    title: 'Bubble Sort',
    category: 'sorting',
    difficulty: 'easy',
    description: 'Return a new list sorted ascending using bubble sort.',
    skeleton: `def bubble_sort(nums):\n    a = list(nums)\n    n = len(a)\n    for i in range(n):\n        for j in range(0, n - i - 1):\n            if a[j] > a[j + 1]:\n                a[j], a[j + 1] = a[j + 1], a[j]\n    return a\n`,
    reference: `def bubble_sort(nums):\n    a = list(nums)\n    n = len(a)\n    for i in range(n):\n        for j in range(0, n - i - 1):\n            if a[j] > a[j + 1]:\n                a[j], a[j + 1] = a[j + 1], a[j]\n    return a\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'bubble_sort([5, 1, 4, 2])', expected: '[1, 2, 4, 5]' },
      { id: 'v2', name: 'sorted', visible: true, expression: 'bubble_sort([1, 2, 3])', expected: '[1, 2, 3]' },
      { id: 'h1', name: 'dupes', visible: false, expression: 'bubble_sort([3, 1, 3])', expected: '[1, 3, 3]' },
    ],
    timeComplexity: 'O(n^2)',
    spaceComplexity: 'O(n)',
    order: 40,
  },
  {
    slug: 'selection-sort',
    title: 'Selection Sort',
    category: 'sorting',
    difficulty: 'easy',
    description: 'Return a new list sorted ascending using selection sort.',
    skeleton: `def selection_sort(nums):\n    a = list(nums)\n    n = len(a)\n    for i in range(n):\n        m = i\n        for j in range(i + 1, n):\n            if a[j] < a[m]:\n                m = j\n        a[i], a[m] = a[m], a[i]\n    return a\n`,
    reference: `def selection_sort(nums):\n    a = list(nums)\n    n = len(a)\n    for i in range(n):\n        m = i\n        for j in range(i + 1, n):\n            if a[j] < a[m]:\n                m = j\n        a[i], a[m] = a[m], a[i]\n    return a\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'selection_sort([64, 25, 12, 22])', expected: '[12, 22, 25, 64]' },
      { id: 'v2', name: 'one', visible: true, expression: 'selection_sort([7])', expected: '[7]' },
      { id: 'h1', name: 'neg', visible: false, expression: 'selection_sort([0, -1, 3])', expected: '[-1, 0, 3]' },
    ],
    timeComplexity: 'O(n^2)',
    spaceComplexity: 'O(n)',
    order: 50,
  },
  {
    slug: 'insertion-sort',
    title: 'Insertion Sort',
    category: 'sorting',
    difficulty: 'easy',
    description: 'Return a new list sorted ascending using insertion sort.',
    skeleton: `def insertion_sort(nums):\n    a = list(nums)\n    for i in range(1, len(a)):\n        key = a[i]\n        j = i - 1\n        while j >= 0 and a[j] > key:\n            a[j + 1] = a[j]\n            j -= 1\n        a[j + 1] = key\n    return a\n`,
    reference: `def insertion_sort(nums):\n    a = list(nums)\n    for i in range(1, len(a)):\n        key = a[i]\n        j = i - 1\n        while j >= 0 and a[j] > key:\n            a[j + 1] = a[j]\n            j -= 1\n        a[j + 1] = key\n    return a\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'insertion_sort([9, 5, 1, 4])', expected: '[1, 4, 5, 9]' },
      { id: 'v2', name: 'sorted', visible: true, expression: 'insertion_sort([1, 2, 3])', expected: '[1, 2, 3]' },
      { id: 'h1', name: 'dupes', visible: false, expression: 'insertion_sort([2, 2, 1])', expected: '[1, 2, 2]' },
    ],
    timeComplexity: 'O(n^2)',
    spaceComplexity: 'O(n)',
    order: 60,
  },
  {
    slug: 'merge-sort',
    title: 'Merge Sort',
    category: 'sorting',
    difficulty: 'medium',
    description: 'Return a new list sorted ascending using merge sort.',
    skeleton: `def merge_sort(nums):\n    if len(nums) <= 1:\n        return list(nums)\n    mid = len(nums) // 2\n    left = merge_sort(nums[:mid])\n    right = merge_sort(nums[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    out = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            out.append(left[i]); i += 1\n        else:\n            out.append(right[j]); j += 1\n    out.extend(left[i:])\n    out.extend(right[j:])\n    return out\n`,
    reference: `def merge_sort(nums):\n    if len(nums) <= 1:\n        return list(nums)\n    mid = len(nums) // 2\n    left = merge_sort(nums[:mid])\n    right = merge_sort(nums[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    out = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            out.append(left[i]); i += 1\n        else:\n            out.append(right[j]); j += 1\n    out.extend(left[i:])\n    out.extend(right[j:])\n    return out\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'merge_sort([38, 27, 43, 3])', expected: '[3, 27, 38, 43]' },
      { id: 'v2', name: 'empty', visible: true, expression: 'merge_sort([])', expected: '[]' },
      { id: 'h1', name: 'dupes', visible: false, expression: 'merge_sort([5, 1, 5, 2])', expected: '[1, 2, 5, 5]' },
    ],
    timeComplexity: 'O(n log n)',
    spaceComplexity: 'O(n)',
    order: 70,
  },
  {
    slug: 'quick-sort',
    title: 'Quick Sort',
    category: 'sorting',
    difficulty: 'medium',
    description: 'Return a new list sorted ascending using quick sort (last element as pivot).',
    skeleton: `def quick_sort(nums):\n    if len(nums) <= 1:\n        return list(nums)\n    pivot = nums[-1]\n    left = [x for x in nums[:-1] if x <= pivot]\n    right = [x for x in nums[:-1] if x > pivot]\n    return quick_sort(left) + [pivot] + quick_sort(right)\n`,
    reference: `def quick_sort(nums):\n    if len(nums) <= 1:\n        return list(nums)\n    pivot = nums[-1]\n    left = [x for x in nums[:-1] if x <= pivot]\n    right = [x for x in nums[:-1] if x > pivot]\n    return quick_sort(left) + [pivot] + quick_sort(right)\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'quick_sort([10, 7, 8, 9, 1])', expected: '[1, 7, 8, 9, 10]' },
      { id: 'v2', name: 'sorted', visible: true, expression: 'quick_sort([1, 2, 3])', expected: '[1, 2, 3]' },
      { id: 'h1', name: 'dupes', visible: false, expression: 'quick_sort([3, 1, 3, 2])', expected: '[1, 2, 3, 3]' },
    ],
    timeComplexity: 'O(n log n) average',
    spaceComplexity: 'O(n)',
    order: 80,
  },
  {
    slug: 'heap-sort',
    title: 'Heap Sort',
    category: 'heaps',
    difficulty: 'medium',
    description: 'Return a new list sorted ascending using heap sort.',
    skeleton: `def heap_sort(nums):\n    a = list(nums)\n    n = len(a)\n\n    def heapify(size, i):\n        largest = i\n        l, r = 2 * i + 1, 2 * i + 2\n        if l < size and a[l] > a[largest]:\n            largest = l\n        if r < size and a[r] > a[largest]:\n            largest = r\n        if largest != i:\n            a[i], a[largest] = a[largest], a[i]\n            heapify(size, largest)\n\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(n, i)\n    for i in range(n - 1, 0, -1):\n        a[0], a[i] = a[i], a[0]\n        heapify(i, 0)\n    return a\n`,
    reference: `def heap_sort(nums):\n    a = list(nums)\n    n = len(a)\n\n    def heapify(size, i):\n        largest = i\n        l, r = 2 * i + 1, 2 * i + 2\n        if l < size and a[l] > a[largest]:\n            largest = l\n        if r < size and a[r] > a[largest]:\n            largest = r\n        if largest != i:\n            a[i], a[largest] = a[largest], a[i]\n            heapify(size, largest)\n\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(n, i)\n    for i in range(n - 1, 0, -1):\n        a[0], a[i] = a[i], a[0]\n        heapify(i, 0)\n    return a\n`,
    testCases: [
      { id: 'v1', name: 'unsorted', visible: true, expression: 'heap_sort([12, 11, 13, 5])', expected: '[5, 11, 12, 13]' },
      { id: 'v2', name: 'one', visible: true, expression: 'heap_sort([4])', expected: '[4]' },
      { id: 'h1', name: 'dupes', visible: false, expression: 'heap_sort([2, 2, 1])', expected: '[1, 2, 2]' },
    ],
    timeComplexity: 'O(n log n)',
    spaceComplexity: 'O(n)',
    order: 90,
  },
  {
    slug: 'linked-list-reversal',
    title: 'Linked List Reversal',
    category: 'linked-lists',
    difficulty: 'easy',
    description: 'Lists are plain Python lists of values. Return a new list with elements reversed (iterative list reversal).',
    skeleton: `def reverse_list(values):\n    out = []\n    i = len(values) - 1\n    while i >= 0:\n        out.append(values[i])\n        i -= 1\n    return out\n`,
    reference: `def reverse_list(values):\n    out = []\n    i = len(values) - 1\n    while i >= 0:\n        out.append(values[i])\n        i -= 1\n    return out\n`,
    testCases: [
      { id: 'v1', name: 'basic', visible: true, expression: 'reverse_list([1, 2, 3, 4])', expected: '[4, 3, 2, 1]' },
      { id: 'v2', name: 'two', visible: true, expression: 'reverse_list([1, 2])', expected: '[2, 1]' },
      { id: 'h1', name: 'empty', visible: false, expression: 'reverse_list([])', expected: '[]' },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(n)',
    order: 100,
  },
  {
    slug: 'tree-preorder',
    title: 'Tree Preorder Traversal',
    category: 'trees-bst',
    difficulty: 'easy',
    description: 'Binary tree nodes are dicts: {"val": n, "left": node|None, "right": node|None}. Return preorder values.',
    skeleton: `def preorder(root):\n    if not root:\n        return []\n    return [root["val"]] + preorder(root.get("left")) + preorder(root.get("right"))\n`,
    reference: `def preorder(root):\n    if not root:\n        return []\n    return [root["val"]] + preorder(root.get("left")) + preorder(root.get("right"))\n`,
    testCases: [
      {
        id: 'v1',
        name: 'balanced',
        visible: true,
        expression: 'preorder({"val": 1, "left": {"val": 2, "left": None, "right": None}, "right": {"val": 3, "left": None, "right": None}})',
        expected: '[1, 2, 3]',
      },
      { id: 'v2', name: 'empty', visible: true, expression: 'preorder(None)', expected: '[]' },
      {
        id: 'h1',
        name: 'skew',
        visible: false,
        expression: 'preorder({"val": 1, "left": None, "right": {"val": 2, "left": None, "right": None}})',
        expected: '[1, 2]',
      },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(h)',
    order: 110,
  },
  {
    slug: 'tree-inorder',
    title: 'Tree Inorder Traversal',
    category: 'trees-bst',
    difficulty: 'easy',
    description: 'Return inorder values for a binary tree of dict nodes.',
    skeleton: `def inorder(root):\n    if not root:\n        return []\n    return inorder(root.get("left")) + [root["val"]] + inorder(root.get("right"))\n`,
    reference: `def inorder(root):\n    if not root:\n        return []\n    return inorder(root.get("left")) + [root["val"]] + inorder(root.get("right"))\n`,
    testCases: [
      {
        id: 'v1',
        name: 'bst',
        visible: true,
        expression: 'inorder({"val": 2, "left": {"val": 1, "left": None, "right": None}, "right": {"val": 3, "left": None, "right": None}})',
        expected: '[1, 2, 3]',
      },
      { id: 'v2', name: 'empty', visible: true, expression: 'inorder(None)', expected: '[]' },
      {
        id: 'h1',
        name: 'left',
        visible: false,
        expression: 'inorder({"val": 2, "left": {"val": 1, "left": None, "right": None}, "right": None})',
        expected: '[1, 2]',
      },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(h)',
    order: 120,
  },
  {
    slug: 'tree-postorder',
    title: 'Tree Postorder Traversal',
    category: 'trees-bst',
    difficulty: 'easy',
    description: 'Return postorder values for a binary tree of dict nodes.',
    skeleton: `def postorder(root):\n    if not root:\n        return []\n    return postorder(root.get("left")) + postorder(root.get("right")) + [root["val"]]\n`,
    reference: `def postorder(root):\n    if not root:\n        return []\n    return postorder(root.get("left")) + postorder(root.get("right")) + [root["val"]]\n`,
    testCases: [
      {
        id: 'v1',
        name: 'balanced',
        visible: true,
        expression: 'postorder({"val": 1, "left": {"val": 2, "left": None, "right": None}, "right": {"val": 3, "left": None, "right": None}})',
        expected: '[2, 3, 1]',
      },
      { id: 'v2', name: 'empty', visible: true, expression: 'postorder(None)', expected: '[]' },
      {
        id: 'h1',
        name: 'skew',
        visible: false,
        expression: 'postorder({"val": 1, "left": None, "right": {"val": 2, "left": None, "right": None}})',
        expected: '[2, 1]',
      },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(h)',
    order: 130,
  },
  {
    slug: 'tree-level-order',
    title: 'Tree Level-Order Traversal',
    category: 'trees-bst',
    difficulty: 'medium',
    description: 'Return level-order (BFS) values for a binary tree of dict nodes.',
    skeleton: `from collections import deque\n\ndef level_order(root):\n    if not root:\n        return []\n    out = []\n    q = deque([root])\n    while q:\n        node = q.popleft()\n        out.append(node["val"])\n        if node.get("left"):\n            q.append(node["left"])\n        if node.get("right"):\n            q.append(node["right"])\n    return out\n`,
    reference: `from collections import deque\n\ndef level_order(root):\n    if not root:\n        return []\n    out = []\n    q = deque([root])\n    while q:\n        node = q.popleft()\n        out.append(node["val"])\n        if node.get("left"):\n            q.append(node["left"])\n        if node.get("right"):\n            q.append(node["right"])\n    return out\n`,
    testCases: [
      {
        id: 'v1',
        name: 'balanced',
        visible: true,
        expression: 'level_order({"val": 1, "left": {"val": 2, "left": None, "right": None}, "right": {"val": 3, "left": None, "right": None}})',
        expected: '[1, 2, 3]',
      },
      { id: 'v2', name: 'empty', visible: true, expression: 'level_order(None)', expected: '[]' },
      {
        id: 'h1',
        name: 'deeper',
        visible: false,
        expression: 'level_order({"val": 1, "left": {"val": 2, "left": {"val": 4, "left": None, "right": None}, "right": None}, "right": {"val": 3, "left": None, "right": None}})',
        expected: '[1, 2, 3, 4]',
      },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(n)',
    order: 140,
  },
  {
    slug: 'dijkstra',
    title: "Dijkstra's Algorithm",
    category: 'graphs',
    difficulty: 'hard',
    description: 'Weighted graph is {node: [(nei, weight), ...]}. Return shortest distances from start as a dict of node -> distance.',
    skeleton: `import heapq\n\ndef dijkstra(graph, start):\n    dist = {start: 0}\n    pq = [(0, start)]\n    while pq:\n        d, u = heapq.heappop(pq)\n        if d > dist.get(u, float("inf")):\n            continue\n        for v, w in graph.get(u, []):\n            nd = d + w\n            if nd < dist.get(v, float("inf")):\n                dist[v] = nd\n                heapq.heappush(pq, (nd, v))\n    return dist\n`,
    reference: `import heapq\n\ndef dijkstra(graph, start):\n    dist = {start: 0}\n    pq = [(0, start)]\n    while pq:\n        d, u = heapq.heappop(pq)\n        if d > dist.get(u, float("inf")):\n            continue\n        for v, w in graph.get(u, []):\n            nd = d + w\n            if nd < dist.get(v, float("inf")):\n                dist[v] = nd\n                heapq.heappush(pq, (nd, v))\n    return dist\n`,
    testCases: [
      {
        id: 'v1',
        name: 'simple',
        visible: true,
        expression: 'dict(sorted(dijkstra({"A": [("B", 1), ("C", 4)], "B": [("C", 2)], "C": []}, "A").items()))',
        expected: "{'A': 0, 'B': 1, 'C': 3}",
      },
      {
        id: 'v2',
        name: 'single',
        visible: true,
        expression: 'dijkstra({"A": []}, "A")',
        expected: "{'A': 0}",
      },
      {
        id: 'h1',
        name: 'path',
        visible: false,
        expression: 'dict(sorted(dijkstra({"A": [("B", 5)], "B": [("C", 1)], "C": []}, "A").items()))',
        expected: "{'A': 0, 'B': 5, 'C': 6}",
      },
    ],
    timeComplexity: 'O((V + E) log V)',
    spaceComplexity: 'O(V)',
    order: 150,
  },
  {
    slug: 'topological-sort',
    title: 'Topological Sort',
    category: 'graphs',
    difficulty: 'medium',
    description: 'Directed graph as adjacency dict. Return one valid topological order as a list (Kahn’s algorithm).',
    skeleton: `from collections import deque, defaultdict\n\ndef topological_sort(graph):\n    indeg = defaultdict(int)\n    for u in graph:\n        indeg.setdefault(u, 0)\n        for v in graph[u]:\n            indeg[v] += 1\n    q = deque([u for u in indeg if indeg[u] == 0])\n    order = []\n    while q:\n        u = q.popleft()\n        order.append(u)\n        for v in graph.get(u, []):\n            indeg[v] -= 1\n            if indeg[v] == 0:\n                q.append(v)\n    return order if len(order) == len(indeg) else []\n`,
    reference: `from collections import deque, defaultdict\n\ndef topological_sort(graph):\n    indeg = defaultdict(int)\n    for u in graph:\n        indeg.setdefault(u, 0)\n        for v in graph[u]:\n            indeg[v] += 1\n    q = deque([u for u in indeg if indeg[u] == 0])\n    order = []\n    while q:\n        u = q.popleft()\n        order.append(u)\n        for v in graph.get(u, []):\n            indeg[v] -= 1\n            if indeg[v] == 0:\n                q.append(v)\n    return order if len(order) == len(indeg) else []\n`,
    testCases: [
      { id: 'v1', name: 'chain', visible: true, expression: 'topological_sort({"A": ["B"], "B": ["C"], "C": []})', expected: "['A', 'B', 'C']" },
      { id: 'v2', name: 'branch', visible: true, expression: 'topological_sort({"A": ["C"], "B": ["C"], "C": []})', expected: "['A', 'B', 'C']" },
      { id: 'h1', name: 'cycle', visible: false, expression: 'topological_sort({"A": ["B"], "B": ["A"]})', expected: '[]' },
    ],
    timeComplexity: 'O(V + E)',
    spaceComplexity: 'O(V)',
    order: 160,
  },
  {
    slug: 'union-find',
    title: 'Union-Find',
    category: 'graphs',
    difficulty: 'medium',
    description: 'Implement Union-Find with path compression. connected(a,b) after unions should reflect components.',
    skeleton: `class UnionFind:\n    def __init__(self, n):\n        self.p = list(range(n))\n\n    def find(self, x):\n        if self.p[x] != x:\n            self.p[x] = self.find(self.p[x])\n        return self.p[x]\n\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra != rb:\n            self.p[rb] = ra\n\n    def connected(self, a, b):\n        return self.find(a) == self.find(b)\n`,
    reference: `class UnionFind:\n    def __init__(self, n):\n        self.p = list(range(n))\n\n    def find(self, x):\n        if self.p[x] != x:\n            self.p[x] = self.find(self.p[x])\n        return self.p[x]\n\n    def union(self, a, b):\n        ra, rb = self.find(a), self.find(b)\n        if ra != rb:\n            self.p[rb] = ra\n\n    def connected(self, a, b):\n        return self.find(a) == self.find(b)\n`,
    testCases: [
      {
        id: 'v1',
        name: 'basic',
        visible: true,
        expression: '(lambda uf: (uf.union(0, 1), uf.union(1, 2), uf.connected(0, 2), uf.connected(0, 3)))(UnionFind(4))[2:]',
        expected: '(True, False)',
      },
      {
        id: 'v2',
        name: 'self',
        visible: true,
        expression: 'UnionFind(3).connected(1, 1)',
        expected: 'True',
      },
      {
        id: 'h1',
        name: 'sep',
        visible: false,
        expression: '(lambda uf: (uf.union(0, 1), uf.connected(2, 3)))(UnionFind(4))[1]',
        expected: 'False',
      },
    ],
    timeComplexity: 'O(α(n)) per op',
    spaceComplexity: 'O(n)',
    order: 170,
  },
  {
    slug: 'kadane',
    title: "Kadane's Algorithm",
    category: 'dynamic-programming',
    difficulty: 'medium',
    description: 'Return the maximum subarray sum for a non-empty list of integers.',
    skeleton: `def max_subarray(nums):\n    best = cur = nums[0]\n    for x in nums[1:]:\n        cur = max(x, cur + x)\n        best = max(best, cur)\n    return best\n`,
    reference: `def max_subarray(nums):\n    best = cur = nums[0]\n    for x in nums[1:]:\n        cur = max(x, cur + x)\n        best = max(best, cur)\n    return best\n`,
    testCases: [
      { id: 'v1', name: 'classic', visible: true, expression: 'max_subarray([-2, 1, -3, 4, -1, 2, 1, -5, 4])', expected: '6' },
      { id: 'v2', name: 'all-neg', visible: true, expression: 'max_subarray([-3, -1, -2])', expected: '-1' },
      { id: 'h1', name: 'single', visible: false, expression: 'max_subarray([5])', expected: '5' },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(1)',
    order: 180,
  },
  {
    slug: 'two-pointers',
    title: 'Two Pointers',
    category: 'arrays-strings',
    difficulty: 'easy',
    description: 'Given a sorted ascending list, return True if any pair sums to target (two-pointer scan).',
    skeleton: `def has_pair_sum(nums, target):\n    lo, hi = 0, len(nums) - 1\n    while lo < hi:\n        s = nums[lo] + nums[hi]\n        if s == target:\n            return True\n        if s < target:\n            lo += 1\n        else:\n            hi -= 1\n    return False\n`,
    reference: `def has_pair_sum(nums, target):\n    lo, hi = 0, len(nums) - 1\n    while lo < hi:\n        s = nums[lo] + nums[hi]\n        if s == target:\n            return True\n        if s < target:\n            lo += 1\n        else:\n            hi -= 1\n    return False\n`,
    testCases: [
      { id: 'v1', name: 'yes', visible: true, expression: 'has_pair_sum([1, 2, 3, 4, 6], 10)', expected: 'True' },
      { id: 'v2', name: 'no', visible: true, expression: 'has_pair_sum([1, 2, 3, 9], 8)', expected: 'False' },
      { id: 'h1', name: 'edges', visible: false, expression: 'has_pair_sum([1, 5], 6)', expected: 'True' },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(1)',
    order: 190,
  },
  {
    slug: 'sliding-window',
    title: 'Sliding Window',
    category: 'arrays-strings',
    difficulty: 'medium',
    description: 'Return the maximum sum of any contiguous subarray of length k (fixed window).',
    skeleton: `def max_window_sum(nums, k):\n    if k <= 0 or k > len(nums):\n        return 0\n    window = sum(nums[:k])\n    best = window\n    for i in range(k, len(nums)):\n        window += nums[i] - nums[i - k]\n        best = max(best, window)\n    return best\n`,
    reference: `def max_window_sum(nums, k):\n    if k <= 0 or k > len(nums):\n        return 0\n    window = sum(nums[:k])\n    best = window\n    for i in range(k, len(nums)):\n        window += nums[i] - nums[i - k]\n        best = max(best, window)\n    return best\n`,
    testCases: [
      { id: 'v1', name: 'basic', visible: true, expression: 'max_window_sum([2, 1, 5, 1, 3, 2], 3)', expected: '9' },
      { id: 'v2', name: 'k1', visible: true, expression: 'max_window_sum([4, 2, 10], 1)', expected: '10' },
      { id: 'h1', name: 'full', visible: false, expression: 'max_window_sum([1, 2, 3], 3)', expected: '6' },
    ],
    timeComplexity: 'O(n)',
    spaceComplexity: 'O(1)',
    order: 200,
  },
]

export const ALGORITHM_CATEGORIES_LIST = [
  'arrays-strings',
  'searching',
  'sorting',
  'linked-lists',
  'stacks-queues',
  'trees-bst',
  'heaps',
  'graphs',
  'recursion-backtracking',
  'dynamic-programming',
  'greedy',
  'bit-manipulation',
]
